---
sidebar_position: 3
---

# Mac-Side Setup

## Step 1: Create SSH ControlMaster config

Edit `~/.ssh/config` (create it if it doesn't exist):

```
# ============================================================
# SSH multiplexing — reuse authenticated connections
# ============================================================
Host *
    ControlMaster auto
    ControlPath ~/.ssh/sockets/%r@%h-%p
    ControlPersist 16h

# ============================================================
# Cloud Dev Server alias
# ============================================================
Host clouddev
    HostName <casaos-ip>
    Port 3434
    User abc
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

Create the sockets directory:

```bash
mkdir -p ~/.ssh/sockets
chmod 700 ~/.ssh/sockets
```

**What this does:**
- When you run `ssh clouddev`, you enter your password once
- SSH creates a socket file at `~/.ssh/sockets/abc@<ip>-3434`
- For the next 16 hours, any `ssh`, `scp`, or `rsync` to the same host:port:user combo skips authentication entirely and uses the open tunnel
- `ServerAliveInterval 60` sends a keepalive every 60 seconds so the tunnel doesn't die from inactivity

## Step 2: Global .gitignore (safety net)

Prevents IDE config files from ever being committed to any Git repo:

```bash
touch ~/.gitignore_global

cat >> ~/.gitignore_global << 'EOF'
# VS Code
.vscode/
*.code-workspace

# General IDE
.idea/
.DS_Store
*.swp
*.swo
*~
EOF

git config --global core.excludesfile ~/.gitignore_global
```

## Step 3: Install VS Code extension

```bash
code --install-extension emeraldwalk.runonsave
```

Or install via VS Code: `Cmd+Shift+X` → search "Run on Save" by emeraldwalk.

## Step 4: Create a workspace file

This is the key file that maps your local project to the server. It lives **outside** your project folder (e.g., in `~/Documents/Workspaces/`) so it can never be committed to Git.

Create `~/Documents/Workspaces/cloud-dev.code-workspace`:

```json
{
    "folders": [
        {
            "name": "my-project",
            "path": "/Users/<your-username>/Code/my-project"
        }
    ],
    "settings": {
        "files.autoSave": "onFocusChange",
        "emeraldwalk.runonsave": {
            "commands": [
                {
                    "match": ".*",
                    "cmd": "rsync -avz --delete -e 'ssh -p 3434' /Users/<your-username>/Code/my-project/ abc@<casaos-ip>:/projects/my-project/"
                }
            ]
        }
    }
}
```

**Replace:**
- `/Users/<your-username>/Code/my-project` → the local path to your project (both occurrences)
- `<casaos-ip>` → your CasaOS server IP
- `/projects/my-project/` → the desired path on the server

**How it works:**
- You edit files in your local folder normally
- When you click away from a file, VS Code autosaves → Run on Save fires `rsync`
- `rsync` compares your entire local project against the server copy
- Only changed files are transferred (incremental — not the whole project each time)
- `--delete` means: if you deleted a file locally, it's deleted on the server too
- rsync rides the already-open SSH ControlMaster socket → no password prompt, no 2FA

:::danger Trailing slash matters
`rsync /path/my-project/` (with trailing slash) copies the **contents** of the directory. `rsync /path/my-project` (no trailing slash) copies the **directory itself**. The workspace config uses a trailing slash on the source. Get this wrong and you'll get `/projects/my-project/my-project/` on the server.
:::

### Why `onFocusChange` and not `afterDelay`

- `afterDelay` saves every second you pause typing → hundreds of rsync calls per minute → server gets hammered
- `onFocusChange` saves only when you click away from the editor (to terminal, another tab, browser) → one upload per editing session per file → clean

### Why `rsync` instead of `scp`

- `scp` only uploads files — deletions are lost. You'd have to manually clean up the server
- `rsync --delete` gives you a true mirror: create, edit, delete locally → reflected on the server
- `rsync` sends only changed blocks, not entire files — faster for large files
- `rsync` scans the full project on every run — for typical projects this takes under a second

## Step 5: Open the workspace

Instead of `File → Open Folder`, use:
- Double-click `cloud-dev.code-workspace` in Finder
- Or `File → Open Workspace from File...` in VS Code
